__pycache__/
*.pyc
*.pyo
.vscode/
.idea/
Thumbs.db
external/
.venv/

# Large inputs stay out; the records that make their retrieval reproducible do not.
data/*
!data/inria_index/
!data/inria_selected/
data/inria_index/*/*
data/inria_selected/*/*
!data/inria_index/
!data/windows/
!data/p1/
data/windows/*
data/p1/*
!data/inria_index/*/index.jsonl
!data/inria_selected/*/manifest.jsonl
!data/inria_selected/*/attempts.jsonl
# Evaluation windows carry over from the preliminary study; kept for reuse.
!data/windows/*.json
# Raw measurements are the study's own output and belong in the reproduction package.
!data/p1/*.json

# Raw *.headers stay local: successful responses carry a pre-signed S3 URL.
# attempts.jsonl records each failure instead.
